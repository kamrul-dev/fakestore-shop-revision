import React from 'react';

const Product = (props) => {
    const {image, title, price, description} = props.Product;
    return (
        <div>
            
        </div>
    );
};

export default Product;